import React from 'react'; import { cn } from '../../lib/utils/cn'; import { InputProps } from '../../types'; export default function Input({ className, label, error, ...props }: InputProps): React.ReactElement { return ( <div className='space-y-2'> {label && ( <label className='block text-sm font-medium text-gray-700'>{label}</label> )} <input className={cn( 'block w-full rounded-md border-gray-300 shadow-sm focus:border-brand-500 focus:ring-brand-500 sm:text-sm', { 'border-red-300 focus:border-red-500 focus:ring-red-500': error }, className )} {...props} /> {error && <p className='text-sm text-red-600'>{error}</p>} </div> ); }
